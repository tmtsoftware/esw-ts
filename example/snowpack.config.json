{
  "devOptions": {
    "port": 3000,
    "bundle": false,
    "open": "none"
  },
  "exclude": ["**/node_modules/**/*"],
  "scripts": {
    "mount:public": "mount public --to /",
    "mount:src": "mount src --to /_dist_",
    "run:tsc": "tsc --noEmit"
  }
}
